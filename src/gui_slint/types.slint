export struct FileEntry {
    name: string,
    size: string,
    modified: string,
    created: string,
    is_dir: bool,
    selected: bool,
    media_kind: string,
    media_date: string,
    metadata_status: string,
    duplicate_name: bool,
    duplicate_size: bool,
    duplicate_modified: bool,
    duplicate_media_date: bool,
}
